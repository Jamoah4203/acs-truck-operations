import {jsPDF} from 'jspdf';
import autoTable from 'jspdf-autotable';
import {supabase} from '../lib/supabase';
import {dateLabel,money} from './format';

async function logoData(path?:string){if(!path)return null;try{const{data}=await supabase.storage.from('company-assets').createSignedUrl(path,300);if(!data?.signedUrl)return null;const blob=await fetch(data.signedUrl).then(r=>r.blob());if(blob.type.includes('svg')){const text=await blob.text();const svg=new Blob([text],{type:'image/svg+xml'});const url=URL.createObjectURL(svg);const img=new Image();await new Promise<void>((res,rej)=>{img.onload=()=>res();img.onerror=rej;img.src=url});const c=document.createElement('canvas');c.width=Math.max(400,img.width||400);c.height=Math.max(200,img.height||200);const ctx=c.getContext('2d')!;ctx.drawImage(img,0,0,c.width,c.height);URL.revokeObjectURL(url);return c.toDataURL('image/png')}return await new Promise<string>((res,rej)=>{const fr=new FileReader();fr.onload=()=>res(String(fr.result));fr.onerror=rej;fr.readAsDataURL(blob)})}catch{return null}}
function header(d:jsPDF,company:any,title:string,subtitle:string){const W=d.internal.pageSize.getWidth();d.setFillColor(15,23,42);d.rect(0,0,W,10,'F');d.setTextColor(15,23,42);d.setFont('helvetica','bold');d.setFontSize(17);d.text(company.name||'Avenue Construction Supply GH Ltd',18,22,{maxWidth:100});d.setFont('helvetica','normal');d.setFontSize(8);d.setTextColor(100,116,139);d.text([company.address,company.phone,company.email,company.website].filter(Boolean).join(' | '),18,29,{maxWidth:110});d.setTextColor(15,23,42);d.setFont('helvetica','bold');d.setFontSize(19);d.text(title,W-18,21,{align:'right'});d.setFont('helvetica','normal');d.setFontSize(8);d.setTextColor(100,116,139);d.text(subtitle,W-18,29,{align:'right'});d.setDrawColor(245,158,11);d.setLineWidth(1);d.line(18,38,W-18,38)}
async function addLogo(d:jsPDF,company:any){const data=await logoData(company.logo_path);if(data)try{d.addImage(data,'PNG',18,13,20,16)}catch{}}
function footer(d:jsPDF,company:any){const W=d.internal.pageSize.getWidth(),H=d.internal.pageSize.getHeight(),pages=(d as any).internal.getNumberOfPages();for(let i=1;i<=pages;i++){d.setPage(i);d.setDrawColor(226,232,240);d.line(18,H-20,W-18,H-20);d.setFontSize(7.5);d.setTextColor(100,116,139);d.text([company.trading_name||company.name||'ACS Truck Operations',company.phone,company.email].filter(Boolean).join(' | '),18,H-14,{maxWidth:140});d.text(`Page ${i} of ${pages}`,W-18,H-14,{align:'right'});d.text('Generated by ACS Truck Operations',18,H-9)}}

export async function invoicePdf(delivery:any,company:any){const d=new jsPDF('p','mm','a4');header(d,company,'INVOICE',delivery.delivery_number);await addLogo(d,company);const customer=delivery.customers||{};d.setFontSize(9);d.setTextColor(15,23,42);d.setFont('helvetica','bold');d.text('BILL TO',18,49);d.setFont('helvetica','normal');d.text(customer.name||'Customer',18,56);d.setFontSize(8);d.setTextColor(100,116,139);d.text([customer.address,customer.phone,customer.email].filter(Boolean).join(' | ')||'—',18,62,{maxWidth:85});d.setTextColor(15,23,42);d.setFont('helvetica','bold');d.text('INVOICE DETAILS',118,49);d.setFont('helvetica','normal');d.text(`Date: ${dateLabel(delivery.delivery_date)}`,118,56);d.text(`Payment: ${String(delivery.payment_status||'unpaid').replace(/_/g,' ')}`,118,62);autoTable(d,{startY:72,margin:{left:18,right:18},head:[['Service / Description','Route','Amount']],body:[[delivery.description||'Delivery / transport service',`${delivery.from_location||'—'} → ${delivery.to_location||'—'}`,money(delivery.expected_income)]],headStyles:{fillColor:[15,23,42]},styles:{fontSize:9,cellPadding:4},columnStyles:{2:{halign:'right'}}});let y=(d as any).lastAutoTable.finalY+8;d.setFont('helvetica','bold');d.setFontSize(11);d.text(`TOTAL: ${money(delivery.expected_income)}`,192,y,{align:'right'});if(company.invoice_terms){y+=15;d.setFontSize(9);d.text('TERMS & CONDITIONS',18,y);d.setFont('helvetica','normal');d.setFontSize(8);d.setTextColor(71,85,105);d.text(company.invoice_terms,18,y+6,{maxWidth:174})}footer(d,company);d.save(`${company.invoice_prefix||'INV'}-${delivery.delivery_number}.pdf`)}

export async function pnlPdf(report:any,company:any,start:string,end:string){const d=new jsPDF('p','mm','a4');header(d,company,'PROFIT & LOSS','Management Statement');await addLogo(d,company);const c=report.current,p=report.previous,net=Number(c.income)-Number(c.expenses),pnet=Number(p.income)-Number(p.expenses);d.setFontSize(8);d.setTextColor(100,116,139);d.text(`${dateLabel(start)} to ${dateLabel(end)}`,192,35,{align:'right'});autoTable(d,{startY:47,margin:{left:18,right:18},head:[['Metric','Current','Previous']],body:[['Income',money(c.income),money(p.income)],['Expenses',money(c.expenses),money(p.expenses)],['Net profit / (loss)',money(net),money(pnet)],['Receivables',money(c.receivables),money(p.receivables)]],headStyles:{fillColor:[15,23,42]},styles:{fontSize:9,cellPadding:3},columnStyles:{1:{halign:'right'},2:{halign:'right'}}});let y=(d as any).lastAutoTable.finalY+10;autoTable(d,{startY:y,margin:{left:18,right:18},head:[['Income category','Amount']],body:[...report.income_categories.map((x:any)=>[x.name,money(x.amount)]),['TOTAL INCOME',money(c.income)]],headStyles:{fillColor:[22,101,52]},styles:{fontSize:9,cellPadding:3},columnStyles:{1:{halign:'right'}}});y=(d as any).lastAutoTable.finalY+10;autoTable(d,{startY:y,margin:{left:18,right:18},head:[['Expense category','Amount']],body:[...report.expense_categories.map((x:any)=>[x.name,money(x.amount)]),['TOTAL EXPENSES',money(c.expenses)],['NET PROFIT / (LOSS)',money(net)]],headStyles:{fillColor:[180,35,24]},styles:{fontSize:9,cellPadding:3},columnStyles:{1:{halign:'right'}}});footer(d,company);d.save(`ACS-PnL-${start}-${end}.pdf`)}
