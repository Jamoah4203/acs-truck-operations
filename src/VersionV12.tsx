import {useEffect} from 'react';
export default function VersionV12(){useEffect(()=>{const apply=()=>{document.querySelectorAll('.prod-brand small').forEach(el=>{if(el.textContent?.includes('Operations'))el.textContent='Operations · v1.2.1'});document.querySelectorAll('.prod-side>.prod-user+small,.prod-side>small').forEach(el=>{if(/^v\d/.test(el.textContent||''))el.textContent='v1.2.1'})};apply();const o=new MutationObserver(apply);o.observe(document.body,{subtree:true,childList:true,characterData:true});return()=>o.disconnect()},[]);return null}
